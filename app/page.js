'use client';
import {useState} from 'react';
const nav=['Dashboard','Employees','Companies','Projects','Pipeline','Follow-ups','Contracts','Collections','KPIs','Reports'];
export default function Home(){const [active,setActive]=useState('Dashboard');const [open,setOpen]=useState(false);return <main className="app"><header className="topbar"><div className="brand">EMDAD <span className="muted">CRM</span></div><div className="actions"><button className="btn btn-secondary">EN / AR</button><button className="btn btn-primary" onClick={()=>setOpen(true)}>+ Quick Action</button></div></header><div className="shell"><aside className="sidebar"><nav className="nav">{nav.map(n=><button key={n} className={active===n?'active':''} onClick={()=>setActive(n)}>{n}</button>)}</nav></aside><section className="content"><h1 className="page-title">{active}</h1><p className="muted">EMDAD Engineering Solutions — Sales CRM</p><div className="cards"><Metric label="Employees" value="0"/><Metric label="Companies" value="0"/><Metric label="Projects" value="0"/><Metric label="Pipeline Value" value="EGP 0"/></div><div className="panel"><div className="actions"><button className="btn btn-primary" onClick={()=>setOpen(true)}>+ Add {active==='Dashboard'?'Project':active.slice(0,-1)}</button></div><p className="muted" style={{marginTop:20}}>Foundation is ready. Business modules will be connected to the new database in the next phase.</p></div></section></div>{open&&<div className="modal-backdrop" onMouseDown={()=>setOpen(false)}><div className="modal" onMouseDown={e=>e.stopPropagation()}><h2 style={{marginTop:0}}>Quick Action</h2><p className="muted">This professional popup system is ready for the CRM actions.</p><div className="form-grid"><div className="field"><label>Action</label><select><option>Add Project</option><option>Add Company</option><option>Add Employee</option><option>Add Follow-up</option><option>Add Collection</option></select></div><div className="actions"><button className="btn btn-secondary" onClick={()=>setOpen(false)}>Cancel</button><button className="btn btn-primary" onClick={()=>setOpen(false)}>Continue</button></div></div></div></div>}</main>}
function Metric({label,value}){return <div className="card"><div className="card-label">{label}</div><div className="card-value">{value}</div></div>}
