# Production CRM

Open the Vercel production deployment connected to this repository.

Use the existing Admin account to sign in. Employees can register and remain pending until activated by an administrator.

Do not use the legacy `/setup` flow when an Admin profile already exists.
